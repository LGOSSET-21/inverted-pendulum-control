const assert=require('node:assert/strict'),{liveStep,liveControl}=require('../visualization/live-physics.js'),D=require('../results/triple/experiments.json');
const turns=[0,0,2*Math.PI,0,-2*Math.PI,0,4*Math.PI,0];assert(Math.abs(liveControl(turns,D.gain,null,20))<1e-8,'Equivalent upright configurations must not command full force');
for(const point of [1,2,3]){let s=Array(8).fill(0),peakX=0,forceOutside=0;for(let i=0;i<4000;i++){s=liveStep(s,D.gain,i<800?{point,x:.1,y:point/6}:null,.00125);peakX=Math.max(peakX,Math.abs(s[0]));assert(s.every(Number.isFinite));if([2,4,6].some(k=>Math.abs(Math.atan2(Math.sin(s[k]),Math.cos(s[k])))>.45))forceOutside=Math.max(forceOutside,Math.abs(liveControl(s,D.gain,null,20)-Math.max(-2,Math.min(2,-4*s[1]-2*s[0]))))}assert(peakX<2,'Cart must not be driven off track by a fallen-state LQR');assert(forceOutside<1e-8,'Cart must use limited braking outside the local angle envelope')}
console.log('Angle wrapping and controller disengagement prevent runaway under sustained pulls.');
